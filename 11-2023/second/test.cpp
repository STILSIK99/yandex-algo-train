#include <iostream>
#include <cstring>
#include <bits/stdc++.h>

using namespace std;

int main(){
    ifstream f1 ("out.txt", ios_base::in), f2 ("out2.txt", ios_base::in);
    set<pair<int, int>> arr;
    while(!f2.eof()){
        static int x, y;
        
    }

    return 0;
}